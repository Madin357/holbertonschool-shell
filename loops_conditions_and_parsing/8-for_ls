#!/usr/bin/env bash
# This script lists files, showing only the part after the first dash

for file in *
do
    # Remove everything up to the first dash (including the dash)
    cleaned="${file#*-}"

    echo "$cleaned"
done

