#!/usr/bin/env bash
# Display filenames but only keep the part after the first dash

for f in *
do
    if [[ "$f" == *-* ]]; then
        # Has a dash → remove the part before the first dash
        echo "${f#*-}"
    else
        # No dash → print whole filename
        echo "$f"
    fi
done

