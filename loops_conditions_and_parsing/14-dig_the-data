#!/usr/bin/env bash
# This script groups Apache log entries by IP and HTTP code, showing occurrence counts

awk '{count[$1" "$9]++} END {for (i in count) print count[i], i}' apache-access.log | sort -nr
