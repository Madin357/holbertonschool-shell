#!/usr/bin/env bash
# This script groups Apache log entries by IP and HTTP code, showing occurrence counts

awk '{ key=$1" "$9; count[key]++ }
     END {
         for (k in count)
             printf "%8d %s\n", count[k], k
     }' apache-access.log | sort -nr

